{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/not-hanjo-mei/ezyapper/main/examples/plugins/antispam-go/config.schema.json",
  "title": "AntiSpam Go Plugin Configuration",
  "description": "JSON Schema for antispam-go plugin config.yaml. Mirrors fileConfig validation constraints.",
  "type": "object",
  "required": [
    "max_messages",
    "time_window_seconds",
    "ignore_bots",
    "log_detection"
  ],
  "additionalProperties": false,
  "properties": {
    "max_messages": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of messages allowed within the time window"
    },
    "time_window_seconds": {
      "type": "integer",
      "minimum": 1,
      "description": "Sliding time window in seconds for rate limiting"
    },
    "ignore_bots": {
      "type": "boolean",
      "description": "Whether to skip rate limiting for bot messages"
    },
    "log_detection": {
      "type": "boolean",
      "description": "Whether to log when spam is detected"
    }
  }
}
